# No third-party dependencies (standard library only, by design -- see README-JA.md).
# This file exists only so PaaS platforms whose default Python build command is
# `pip install -r requirements.txt` (e.g. Render) don't fail when it's missing.
